%Math 1371 Assignment 1
%Luke Reimer

format long;

radian_angle = pi/2
degree_angle = 67

calculate_radians = (sin(radian_angle))^2 + (cos(radian_angle))^2;
calculate_degrees = (sind(degree_angle))^2 + (cosd(degree_angle))^2;

display(calculate_radians);
display(calculate_degrees);